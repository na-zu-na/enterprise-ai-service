services:
  - type: web
    name: pe6203-enterprise-assistant-demo
    runtime: python
    rootDir: demo_backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
    plan: free
    healthCheckPath: /health
